import { getAllPostsService } from '../../src/services/post.service';

describe('getAllPostsService', () => {
  it('should be defined', () => {
    expect(getAllPostsService).toBeDefined();
  });
});
