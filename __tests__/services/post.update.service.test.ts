import { updatePostService } from '../../src/services/post.service';

describe('updatePostService', () => {
    it('should be defined', () => {
      expect(updatePostService).toBeDefined();
    });
  });